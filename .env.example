NODE_ENV=production
PORT=5000
# Only provider cooldown/block state, not hotel/search data.
PROVIDER_STATE_FILE=./var/provider-state.json
# Live access remains unavailable pending verified authorization and adapter docs.
